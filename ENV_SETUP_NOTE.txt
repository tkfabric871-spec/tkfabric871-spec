REAL .env NOTICE
================
The real backend/.env is intentionally not included in this ZIP because the
accessible project archive did not contain it. Use backend/.env.example as the
template and keep the real .env private.

Required values:
PORT=4000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=YOUR_MYSQL_PASSWORD
DB_NAME=smart_invest
JWT_SECRET=YOUR_LONG_RANDOM_SECRET
ADMIN_USERNAME=YOUR_ADMIN_USERNAME
ADMIN_PASSWORD=YOUR_ADMIN_PASSWORD
